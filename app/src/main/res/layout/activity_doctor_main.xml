<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- Top wave shape decoration -->
    <View
        android:id="@+id/viewTopWave"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:background="@drawable/top_wave_bg"
        app:layout_constraintTop_toTopOf="parent"/>

    <!-- Tiêu đề "Bảng điều khiển Bác sĩ" -->
    <TextView
        android:id="@+id/tvDoctorDashboardTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Bảng điều khiển Bác sĩ"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        app:layout_constraintTop_toBottomOf="@+id/viewTopWave"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"/>

    <!-- Card cho các chức năng -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardDoctorFunctions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginTop="16dp"
        app:cardCornerRadius="15dp"
        app:cardElevation="8dp"
        app:layout_constraintTop_toBottomOf="@id/tvDoctorDashboardTitle">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Nút Xem lịch khám -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnViewAppointments"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:text="Xem lịch khám"
                android:textSize="16sp"
                android:textAllCaps="false"
                android:backgroundTint="#18A76A"
                android:textColor="@android:color/white"
                app:cornerRadius="10dp"
                android:layout_marginBottom="16dp"/>

            <!-- Nút Xem danh sách bệnh nhân -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnViewPatients"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:text="Xem danh sách bệnh nhân"
                android:textSize="16sp"
                android:textAllCaps="false"
                android:backgroundTint="#18A76A"
                android:textColor="@android:color/white"
                app:cornerRadius="10dp"
                android:layout_marginBottom="16dp"/>

            <!-- Nút Cập nhật thông tin cá nhân -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnEditProfile"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:text="Cập nhật thông tin cá nhân"
                android:textSize="16sp"
                android:textAllCaps="false"
                android:backgroundTint="#18A76A"
                android:textColor="@android:color/white"
                app:cornerRadius="10dp"/>
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- Nút Đăng xuất -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnLogout"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="24dp"
        android:text="Đăng xuất"
        android:textSize="16sp"
        android:textAllCaps="false"
        android:backgroundTint="#FF0000"
        android:textColor="@android:color/white"
        app:cornerRadius="10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>